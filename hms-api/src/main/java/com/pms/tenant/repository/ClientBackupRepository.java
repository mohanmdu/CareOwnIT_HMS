package com.pms.tenant.repository;

import com.pms.tenant.entity.ClientBackup;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClientBackupRepository extends JpaRepository<ClientBackup, Long> {
}
