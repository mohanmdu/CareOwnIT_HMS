package com.pms.tenant.entity;

/** Manual follow-up tracking for a submitted "Request Demo" enquiry - set by whoever on the sales team is working the lead, not by the applicant. */
public enum ContactEnquiryStatus {
    NEW,
    CONTACTED,
    CLOSED
}
