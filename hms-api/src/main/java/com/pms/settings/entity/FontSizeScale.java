package com.pms.settings.entity;

/** Maps to a --hms-font-scale multiplier applied to the --hms-font-size-* scale - not a free numeric input. */
public enum FontSizeScale {
    COMPACT,
    COMFORTABLE,
    SPACIOUS
}
